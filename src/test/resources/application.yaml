spring:
  application.name: WelDemo

  flyway:
    url: "jdbc:sqlite::memory"
    user: sa
    password: sa

  datasource:
    url: "jdbc:sqlite::memory"
    username: sa
    password: sa

logging:
  level:
    com.demo.wel: debug
    org.springframework.security: debug

